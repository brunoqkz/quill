name: Trigger Workflow

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Echo message
        run: echo "This is a trigger workflow to help GitHub discover all workflows"
